# الحالة الحالية

آخر تحقق: 2026-08-28 على `develop` عند `16a8a6d` (`chore: sync Haitham upstream through 5dbed6d`). كان الفرع مطابقًا لـ`origin/develop`، ومرجع `upstream/main` المحلي `5dbed6d` مدمجًا.

## يعمل الآن

- الأرشيف العام والتصفح وخط ingestion والفهرسة والبحث الحرفي/الدلالي.
- Auth بالبريد/كلمة المرور وPKCE، والملف الشخصي مع RLS.
- تقدم محلي للزائر ومزامنة تقدم المسجل عبر RPC آمن.
- مزامنة upstream حتى `5dbed6d`.

## جزئي أو غير موجود

- مساحة الطالب لا تعرض التقدم المخزن ولا توفر تجميعًا أو إحصاءات أو إعادة ضبط.
- لا يوجد نموذج لمسارات الدراسة؛ قوائم التشغيل للتصفح والتسلسل فقط.
- لا توجد Ask AI أو RAG؛ البحث الدلالي استرجاع فقط.
- اختبارات end-to-end لتكامل Supabase المستضاف.

## الخطوة التالية

تحقق من migrations وتدفقات Auth/RPC على Supabase المستضاف، ثم اعرض تقدم الطالب و«تابع من حيث توقفت» في مساحته. بعد إغلاق هذه الحلقة اعتمد نموذج المسارات، ثم Ask AI.

## عوائق ومخاطر نشطة

- لا يثبت المستودع حاليًا تطابق migrations مع Supabase البعيد.
- حفظ أكبر موضع لا يمثل الرجوع المقصود أو إعادة الدراسة.
- المحلي مربوط بـ`videoId` والسحابة بـ`lesson_key`؛ تغيير المصدر أو UUIDs يحتاج ترحيلًا يحفظ تقدم الطلاب.
- المحتوى والبحث يعتمدان على مصادر وخدمات خارجية، ومزامنة upstream اليدوية حساسة خصوصًا في `Player.tsx` وخط البيانات والإعدادات.

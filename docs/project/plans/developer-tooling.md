# خطة: أتمتة التحقق للمطور

- الحالة: `مكتملة`
- المالك: `Codex`
- آخر تحديث: `2026-09-04`
- ملف الوحدة: `docs/project/02-ARCHITECTURE.md`

## الهدف ومعيار الاكتمال

إضافة أوامر الحالة والتحقق والتقرير وتحديث CI، مع نجاح الأوامر وفحص YAML.

## النطاق

- داخل النطاق: scripts وpackage.json وCI وتوثيق سير العمل.
- خارج النطاق: سلوك المنتج وStudentHome وPlayer وSupabase وSlice 5، دون commit أو push.

## الحالة الأساسية

`develop` نظيف عند `9797ffc` ومتزامن حسب مرجع origin المحلي.

## المراحل

- [x] تنفيذ الأدوات وتحديث CI والتوثيق.
- [x] التحقق والمراجعة.

## التحقق

- [x] أوامر الأدوات الثلاثة وفحص Bash وYAML.
- [x] `pnpm check`
- [x] `pnpm exec tsc --noEmit`
- [x] `pnpm build`
- [x] `git diff --check`

## قرارات أو عوائق

قُرئت إعدادات Astro وGitignore وCI الحالي للتأكد من متطلبات البناء وحدود المخرجات. يستخدم CI Node 22 وpnpm المحدد في packageManager. لا تتصل أدوات Git بالخدمات، وتقارير المزامنة تعتمد على المراجع المحلية.

## النتيجة

نجحت أوامر الأدوات الثلاثة وBash وYAML وبوابات التحقق الأربع. ثبت توقف التحقق عند فشل pnpm check ورفض تشغيل الحالة خارج المستودع. استُخدم pnpm 9.12.0 المخزن محليًا عبر PATH مؤقت بعد تعذر فتح قاعدة بيانات المشغل العام؛ احتاج tsx إذن الشبكة لمقبس IPC المحلي. لا تغييرات منتج ولا commit أو push.

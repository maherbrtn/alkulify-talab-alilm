# خطة: أول مسار علمي مراجع — شرح ثلاثة الأصول

- الحالة: `مكتملة`
- المالك: `Codex`
- آخر تحديث: `2026-09-14`
- ملف الوحدة: `docs/project/06-STUDY-PATHS.md`

## الهدف ومعيار الاكتمال

إضافة شرح ثلاثة الأصول إلى الكتالوج العام المحلي بتعريف منشور للإصدار 1، وفق المنهج المراجع الذي قدمه صاحب المشروع: أربع وحدات وسبعة دروس بالترتيب المحدد، مع إثبات الهوية وحل الروابط وmanifest النشر ونجاح بوابات المستودع.

## النطاق

- داخل النطاق: تعريف المنهج الثابت، قائمة الكتالوج، selfchecks، والحالة الحالية ووثيقة المسارات.
- خارج النطاق: إعادة تصميم Study Paths V1، schema أو migrations، تغيير lesson registry أو عقد المصادر، Telegram/corpus، أي اتصال أو mutation مستضاف، وcommit/push.

## الحالة الأساسية

الفرع `develop` نظيف عند `584b071` (`chore: harden global supabase security`). الكتالوج المحلي فارغ والـfixture التقني draft. تحقق تطابق المفاتيح السبعة مع معرّفات الفيديو والعناوين التي قدمها صاحب المشروع في registry وبيانات التصفح الحالية. خطط Study Paths السابقة مغلقة؛ هذه إضافة محتوى محلية مستقلة.

## المراحل

- [x] قراءة العقد والوثائق والاختبارات والتحقق من بيانات الدروس والأنماط القائمة.
- [x] إضافة تعريف مستقل بهويات UUID v4 جديدة وتاريخ نشر canonical، وتوصيله بالكتالوج.
- [x] تحديث assertions المتأثرة وإثبات المنهج والروابط وmanifest مع إبقاء الـfixture خارج الكتالوج.
- [x] تشغيل التحقق المحلي ومراجعة الفرق وتحديث حدود القبول في الوثائق.

## التحقق

- [x] مطابقة الدروس السبعة في `data/lesson-registry.json` و`data/videos.json`.
- [x] فحص manifest النشر والروابط المولدة محليًا.
- [x] `pnpm check`
- [x] `pnpm build`
- [x] `pnpm exec tsc --noEmit`
- [x] `git diff --check`

## قرارات أو عوائق

- العناوين والأهداف من المنهج المراجع المقدم، لا من ASR. مواضع الدروس تبدأ من 1 داخل كل وحدة وفق عقد المجال، مع حفظ الترتيب الإجمالي 1–7.
- توليد الهويات مرة واحدة بـ`node:crypto.randomUUID()` وفق النمط الموجود في `scripts/sync-lesson-registry.ts`، وتثبيت timestamp من `new Date().toISOString()`.
- قراءة `src/lib/lesson-registry.ts` و`src/lib/data.ts` وبيانات الفيديو لتأكيد عقد الهوية وحل الروابط فقط؛ وقراءة قوالب المسارات و`astro.config.*` و`package.json` و`tsconfig.json` لتحديد مخرجات البناء والتحقق الآمن. قُرئ كذلك `StudyPathVersion.astro` للتحقق من ترتيب روابط البناء، وموضع robots في `Base.astro` لتأكيد أن السلوك القائم هو `noindex, follow`.
- `pnpm verify:study-path-publications` يقرأ المستضاف ويحتاج service-role؛ لا يُشغل في هذه المهمة المحلية. تستخدم دوال manifest/verifier النقية مع بيانات اختبار داخل الذاكرة فقط.
- يبقى إدخال سجل النشر المستضاف وقبول المتصفح الإنتاجي المصادق عليه لخطوة لاحقة صريحة.

## النتيجة

اكتملت الإضافة المحلية فقط. التعريف الوحيد «شرح ثلاثة الأصول» منشور بالإصدار 1، بأربع وحدات وسبعة دروس بالترتيب والتقسيم المراجعين. بقي الـfixture draft وخارج الكتالوج، وبقيت data/lesson-registry وschema/migrations ومنطق المنتج دون تعديل.

- `pnpm check`: خروج `0`؛ `Slice 4.1: comment, body-literal, ordered ACL, metadata, and lexical regression checks passed` ثم `selfcheck ok`.
- `pnpm build`: خروج `0`؛ `8262 page(s) built in 31.09s`، ثم نجاح guard. ظهرت صفحتا المنهج والطالب الجديدتان.
- `pnpm exec tsc --noEmit`: خروج `0` بلا تشخيصات.
- `git diff --check`: خروج `0` بلا مخرجات.
- فحص مستقل محلي بـ`node --import tsx` لدوال manifest/verifier وHTML الناتج: خروج `0`؛ ثبتت الروابط السبعة ووجود صفحات الدروس وترتيب الوحدات ومحتواها وكتالوج لوحة الطالب و`noindex, follow` واستبعاد صفحة الطالب من sitemap وغياب الـfixture والنسخ التاريخية غير الموجودة. تطابق projection محلي داخل الذاكرة مع manifest، وأعاد registry فارغ `missing` كما ينبغي. لا يُقرأ المستضاف.
- بيئة الأوامر: استُخدمت نسخة `pnpm 9.12.0` المثبتة مسبقًا المطابقة لـ`packageManager` عبر PATH مؤقت خارج المستودع؛ فشل launcher الافتراضي أولًا بسبب صلاحية cache database. مُنحت صلاحية sockets لتمكين IPC الخاص بـtsx وخادم معالجة الخطوط المحلي الخاص بـAstro بعد فشل `EPERM`؛ نجحت بوابتا pnpm بعدها. نجح `node --import tsx scripts/selfcheck.ts` أيضًا بخروج `0` قبل ذلك. صحح مسبار HTML المؤقت افتراض `nofollow` إلى سياسة `noindex, follow` القائمة بعد فحص `Base.astro`؛ لم يُعدل سلوك المنتج.
- `pathId`: `e17f26d2-2a38-40af-9984-2af8ec45ba43`؛ `publishedAt`: `2026-09-14T16:36:21.235Z`.
- digest المثبت للإصدار 1: `2374ced4629fcafdb1be830231e2eab647d3d7394f167453f68fe1252fc34afb`.

مراجعة الفرق محليًا: اقتصرت تغييرات الكود على التعريف وقائمة الكتالوج والاختبارات المتأثرة؛ لا تغييرات على المجال أو آليات النشر أو UI أو المصدر أو الخدمات. لا commit/push أو نشر بناء أو اتصال/كتابة مستضافة. يبقى إدخال صف النشر المطابق والتحقق الإداري منه ونشر الموقع وقبول المتصفح الإنتاجي المصادق عليه خطوات لاحقة؛ لم يُعلن قبول إنتاجي.

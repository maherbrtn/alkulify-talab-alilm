# تعليمات الوكلاء

الحقيقة الأساسية هي الكود وGit، ثم الوثائق؛ لا تعتمد على ذاكرة المحادثة.

## ترتيب القراءة الإلزامي

ابدأ بهذا الملف، ثم اقرأ فقط بالترتيب:

1. `docs/project/START-HERE.md`
2. `docs/project/03-CURRENT-STATE.md`
3. ملف الوحدة المرتبطة بالمهمة، كما يحدده `START-HERE.md`
4. الخطة النشطة المرتبطة بالمهمة في `docs/project/plans/` إن وجدت

لا تقرأ كل ملفات المشروع أو `docs/project/` افتراضيًا. وسّع السياق فقط عند حاجة واضحة، وسجّل سبب التوسع في الخطة أو التسليم.

## التشغيل

- نفّذ `git status` وتحقق من الفرع قبل أي عمل؛ خط التطوير المعتاد هو `develop`.
- افحص التغييرات الموجودة واحفظ عمل المستخدم؛ لا تحذف أو تستبدل عملًا غير مفهوم.
- اتبع نطاق المهمة، واستخدم `docs/project/WORKFLOW.md` للعمل الممتد أو متعدد المراحل.
- لا تنفذ merge أعمى من `upstream`؛ راجع الفرق والتعارضات الدلالية أولًا.
- لا تعمل commit أو push إلا بطلب صريح.

## التحقق والتسليم

قبل اعتبار تغيير وظيفي مكتملًا شغّل، ما لم يمنعك عائق موثّق:

- `pnpm check`
- `pnpm build`
- `git diff --check`

حدّث `docs/project/03-CURRENT-STATE.md` فقط عندما تتغير الحالة الفعلية أو الخطوة التالية. استخدم قالب `docs/project/handoffs/TEMPLATE.md` عند توقف العمل وفيه ما يحتاجه الوكيل التالي، ولا تحول ملف الحالة إلى سجل يومي.
